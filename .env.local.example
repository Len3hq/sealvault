# Copy this to .env.local and fill in your values

# Privy (Phase 2)
NEXT_PUBLIC_PRIVY_APP_ID=

# OpenAI (Phase 5 - Agent)
OPENAI_API_KEY=

# Pinata — get JWT from app.pinata.cloud → API Keys → New Key (pinFileToIPFS scope)
PINATA_JWT=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

RELAYER_PRIVATE_KEY=
# Public address of the relayer wallet (derived from RELAYER_PRIVATE_KEY)
# Used in queries to reject entities not created by the trusted relayer
NEXT_PUBLIC_RELAYER_ADDRESS=